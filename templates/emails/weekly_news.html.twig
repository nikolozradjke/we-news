<html>
<head>
    <meta charset="UTF-8">
    <title>Weekly Top News</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #2c3e50;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 8px 8px 0 0;
        }
        .content {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 0 0 8px 8px;
        }
        .news-item {
            background-color: white;
            margin-bottom: 20px;
            padding: 15px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        .news-title {
            font-size: 18px;
            font-weight: bold;
            color: #2c3e50;
            margin-bottom: 10px;
        }
        .news-meta {
            color: #666;
            font-size: 14px;
            margin-bottom: 10px;
        }
        .news-excerpt {
            margin-bottom: 10px;
        }
        .comment-count {
            background-color: #3498db;
            color: white;
            padding: 5px 10px;
            border-radius: 15px;
            font-size: 12px;
            display: inline-block;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #ddd;
            color: #666;
            font-size: 12px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>📰 Weekly Top News</h1>
        <p>Week {{ week }}, {{ year }} - Most Commented Articles</p>
    </div>
    
    <div class="content">
        <p>Here are the top 10 most commented news articles from this week:</p>
        
        {% for item in news %}
            <div class="news-item">
                <div class="news-title">
                    {{ loop.index }}. {{ item[0].title }}
                </div>
                
                <div class="news-meta">
                    Published: {{ item[0].createdAt|date('M d, Y') }}
                </div>
                
                <div class="news-excerpt">
                    {{ item[0].shortDescription }}
                </div>
                
                <div class="comment-count">
                    {{ item.commentCount }} comment{{ item.commentCount != 1 ? 's' : '' }}
                </div>
            </div>
        {% endfor %}
    </div>
    
    <div class="footer">
        <p>This is an automated weekly newsletter.</p>
        <p>Stay informed with the most discussed news!</p>
    </div>
</body>
</html>